#include <stdio.h>

int main() {

    int m=0;
    for(int i=5; i<3; i++){
        m++;
    }
    if(m==1){printf("if result is false execute one time");}
    else {printf("no execute"); }

    return 0;
}